# pyvesync canonical request fixtures — LAP-V201S (Vital 200S Air Purifier)
# Source: https://github.com/webdjoe/pyvesync
# Tag: 3.4.2 (commit 01196b9 — 2026-04-16)
# Original path: src/tests/api/vesyncpurifier/LAP-V201S.yaml
#
# Trimmed to json_object.payload only. PyvesyncCoverageSpec reads:
#   [op].json_object.payload.method   — the bypassV2 method name
#   [op].json_object.payload.data     — the request data fields

set_auto_mode:
  json_object:
    payload:
      data:
        workMode: auto
      method: setPurifierMode
      source: APP

set_fan_speed:
  json_object:
    payload:
      data:
        levelIdx: 0
        levelType: wind
        manualSpeedLevel: 3
      method: setLevel
      source: APP

set_manual_mode:
  json_object:
    payload:
      data:
        levelIdx: 0
        levelType: wind
        manualSpeedLevel: 1
      method: setLevel
      source: APP

set_pet_mode:
  json_object:
    payload:
      data:
        workMode: pet
      method: setPurifierMode
      source: APP

set_sleep_mode:
  json_object:
    payload:
      data:
        workMode: sleep
      method: setPurifierMode
      source: APP

turn_off:
  json_object:
    payload:
      data:
        powerSwitch: 0
        switchIdx: 0
      method: setSwitch
      source: APP

turn_off_display:
  json_object:
    payload:
      data:
        screenSwitch: 0
      method: setDisplay
      source: APP

turn_on:
  json_object:
    payload:
      data:
        powerSwitch: 1
        switchIdx: 0
      method: setSwitch
      source: APP

turn_on_display:
  json_object:
    payload:
      data:
        screenSwitch: 1
      method: setDisplay
      source: APP

update:
  json_object:
    payload:
      data: {}
      method: getPurifierStatus
      source: APP
