# Gradle build output and caches
build/
.gradle/
gradle-app.setting
!gradle-wrapper.jar
**/build/
out/

# Python (linter + future Python tooling — uv runs Python via tests/lint.py)
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# IntelliJ IDEA
.idea/
*.iml
*.ipr
*.iws

# Eclipse
.project
.classpath
.settings/
bin/

# VS Code (per-user config)
.vscode/

# Claude Code per-user state (agent defs + project context ARE checked in)
.claude/settings.local.json
.claude/.credentials.json

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor swap / backup files
*.swp
*.swo
*~

# Logs from local test/dev runs
*.log

# Local env files (in case anyone adds CI secrets locally)
.env
.env.local

# Maintainer-local working notes. Public roadmap lives in ROADMAP.md.
TODO.md

# Scratch caches from research agents (HA cross-check, etc.)
.tmp/

# Local dry-run artifact from tools/pyvesync-tracker/detect_devices.py
# (CI writes this in cwd as a body-file for `gh issue create`; ephemeral.)
new-devices.md
